class Solution:
    def canJump(self, nums: List[int]) -> bool:
        distance = nums[0]

        for i in range(1, len(nums)):
            distance -= 1

            if distance < 0:
                return False

            if distance < nums[i]:
                distance = nums[i]

        return True
